@model List<Cliente>

@{
    ViewData["Title"] = "Clientes";
}

<h2>Lista de Clientes</h2>
<a class="btn btn-primary" asp-action="Create">Novo Cliente</a>

<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Telefone</th>
            <th>Endereço</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var cliente in Model)
        {
            <tr>
                <td>@cliente.Nome</td>
                <td>@cliente.Email</td>
                <td>@cliente.Telefone</td>
                <td>@cliente.Endereco</td>
                <td>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@cliente.IdCliente">Editar</a>
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@cliente.IdCliente">Detalhes</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@cliente.IdCliente">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>